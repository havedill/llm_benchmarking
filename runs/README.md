# Runs

Raw benchmark artifacts. Treat this as source-of-truth.

Conventions:

- Run directories are `run_YYYYMMDD_HHMMSSZ`.
- `runs/latest` points to the most recent run directory.
- Do not hand-edit files in run directories.
